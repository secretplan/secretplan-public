namespace DATA_ASSEMBLY.Distributable;

public enum SaveFileVersion
{
    /// <summary>
    ///     Default value, save data was not initialized
    /// </summary>
    Unknown = 0,

    FirstVersion = 1,
}